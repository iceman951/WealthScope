node_modules
.DS_Store

/build
/.svelte-kit
/dist
# Local D1/KV state for `vite dev` and `wrangler dev`. Disposable — `pnpm db:reset`
# deletes it; `pnpm db:migrate` then rebuilds the schema and the next `pnpm dev`
# seeds the demo data.
/.wrangler

# Generated by `wrangler types` from wrangler.jsonc (pnpm cf:types).
/worker-configuration.d.ts

.env
.env.*
!.env.example

/test-results
/playwright-report
/blob-report
/playwright/.cache

*.log
