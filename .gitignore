node_modules
.DS_Store

/build
/.svelte-kit
/dist
/.wrangler

# Local PGlite data directory. Disposable — `pnpm db:reset` deletes it and the
# next `pnpm dev` rebuilds the schema and demo data from scratch.
/.pglite

.env
.env.*
!.env.example

/test-results
/playwright-report
/blob-report
/playwright/.cache

*.log
